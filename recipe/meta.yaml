{% set name = "qcengine" %}
{% set version = "0.7.1" %}
{% set sha256 = "b1ba6f87492a78591e18adaf412cf0277d0e63cf56463a7082d92cf407711e2c" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vvv"

requirements:
  build:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - pydantic >=0.20.*
    - pyyaml
    - py-cpuinfo
    - psutil
    - qcelemental >=0.4.2

test:
  imports:
    - qcengine
  requires:
    - pytest
  commands:
    - pytest --pyargs qcengine

about:
  home: http://github.com/MolSSI/QCEngine
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: 'A wrapper for Quantum Chemistry engines that adheres to the MolSSI (molssi.org) QCSchema.'

  doc_url: https://qcengine.readthedocs.io/en/latest/
  dev_url: http://github.com/MolSSI/QCEngine

extra:
  recipe-maintainers:
    - dgasmith
    - lnaden
